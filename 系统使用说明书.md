# 库存管理系统 (WMS) — 使用说明书

## 1. 快速开始

1. 解压到任意目录（路径不要含中文或空格）
2. 双击 `start.bat`
3. 看到"启动成功"后，浏览器打开 `http://localhost:3001`
4. 使用默认账号登录：**admin / admin123**

> 首次启动会自动创建管理员账号。登录后请立即修改密码。
> 如果忘记密码，关闭服务后双击 `reset-password.bat` 重置为 `admin123`。

---

## 2. 移动端访问

手机连接同一 WiFi，浏览器打开：

```
http://电脑IP:3001
```

电脑 IP 查看方式：`Win+R` → 输入 `cmd` → 输入 `ipconfig` → 看 IPv4 地址。

### 扫码功能

手机浏览器无法调用摄像头（需要 HTTPS）。如需扫码，安装部署包自带的 APK：

1. 手机浏览器打开 `http://电脑IP:3001/apk/wms-standalone.apk` 下载安装
2. 打开 App，点右上角设置图标，输入服务器地址 `http://电脑IP:3001`
3. 登录后即可使用摄像头扫码出入库

---

## 3. 仓库管理

**路径**：侧边栏 → 仓库与库位

- 点击「新建仓库」添加仓库，填写名称和地址
- 点击仓库名称进入库位管理，添加库位（如 A-01、B-03）
- 每个库位自动生成二维码，可打印贴在货架上

---

## 4. 商品管理

### 4.1 商品分类
支持无限层级分类，先建分类再建商品。

### 4.2 商品列表
- 新建商品：SKU 自动生成，填写名称、单位即可
- 支持条码、规格、分类、成本价、售价、安全库存
- **商品图片**：可上传商品照片，库存查询时可看图辨认
- **保质期**：可设置保质期截止日和临期预警天数
- **按仓库设置安全库存**：不同仓库可设不同安全库存值

### 4.3 批量导入
- 下载 Excel 模板 → 填写数据 → 导入，快速批量录入商品和初始库存

---

## 5. 库存管理

- **库存查询**：按仓库筛选，查看实时库存数量和库位分布
- **库存流水**：所有出入库操作的详细记录
- **库存预警**：安全库存不足 + 临期商品自动标红预警

---

## 6. 入库管理

创建入库单 → 选择商品、库位、数量 → 确认入库 → 库存自动增加

---

## 7. 出库管理

创建出库单 → 选择商品、库位、数量 → 确认出库 → 库存自动扣减

---

## 8. 调拨管理

跨仓库调拨：选择源仓库和目标仓库 → 提交审批 → 通过后库存自动转移

---

## 9. 盘点管理

创建盘点任务 → 按库位拆分 → 录入实盘数量 → 差异自动调账

---

## 10. 报表统计

- **出入库报表**：近 30 天出入库趋势图 + 汇总
- **周转率报表**：各仓库库存周转分析

---

## 11. 用户管理

管理员可创建操作员账号，三种角色：

| 角色 | 权限 |
|------|------|
| 仓库管理员 | 全部权限（默认 admin 账号） |
| 操作员 | 只能操作指定的仓库，不能管理用户 |

---

## 12. 数据备份

所有数据存储在 `server\prisma\dev.db`（SQLite 单文件）。

- **备份**：复制 `dev.db` 到安全位置
- **恢复**：把备份的 `dev.db` 放回原位置
- **建议**：每天备份一次

---

## 13. 升级方法

1. 备份 `server\prisma\dev.db`
2. 解压新版部署包覆盖原目录
3. 把备份的 `dev.db` 放回 `server\prisma\`
4. 重新运行 `start.bat`

---

## 14. 常见问题

**Q: 双击 start.bat 闪退？**
A: 路径含中文或空格，移到纯英文路径。或右键以管理员身份运行。

**Q: 手机浏览器打不开？**
A: 检查防火墙是否放行 3001 端口，电脑和手机是否同一 WiFi。或用电脑开热点让手机直连。

**Q: 端口 3001 被占用？**
A: start.bat 会自动清理旧进程。如仍有问题，编辑 `server\.env` 添加 `PORT=3002`。

**Q: 数据库损坏？**
A: 删除 `server\prisma\dev.db`，重新运行 start.bat 自动重建（历史数据会丢失，请定期备份）。

---

版本：v2.3.8 | 2026年5月
